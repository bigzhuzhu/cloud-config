<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.rola.basemvc.mapper.BlogUserMapper">
    <insert id="addBlogUser">
        insert into m_user (id, username, avatar, email, password, status, created, last_login)
        values (null, 'TOMandJACK', 'avatarTOM', 'xxxxx@qq.com', '12314235345', '1', now(), now())
    </insert>

    <select id="getAllUser" resultType="BlogUser">
        select * from m_user;
    </select>
</mapper>